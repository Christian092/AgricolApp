@model IEnumerable<ProyectoV1.Models.procedencia_semen>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Template.cshtml";
}

<h2>Procedencias de Semen</h2>
<hr />
<table class="table table-hover" style="width:auto">
    <tr>
        <th>
            Procedencia de Semen
        </th>
        
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.ActionLink(" ", "Edit", new { id = item.id }, htmlAttributes: new { @class = "btn btn-success glyphicon glyphicon-pencil" })

                @Html.ActionLink(" ", "Delete", new { id = item.id }, htmlAttributes: new { @class = "btn btn-danger glyphicon glyphicon-trash" })
            </td>
        </tr>
    }

</table>
@Html.ActionLink(" Añadir Procedencia de Semen", "Create", " ", htmlAttributes: new { @class = "btn btn-primary glyphicon glyphicon-plus" })
<a href="~/Inseminacion/Create" class="btn btn-success glyphicon glyphicon-arrow-left"> Volver a Inseminacion</a>